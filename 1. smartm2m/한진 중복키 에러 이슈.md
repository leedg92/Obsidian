```
------------------------------------예외 처리(UPDATE)--------------------

public void saveCopino(Map<String, Object> param) {
    try {
        copinoMapper.saveCopino(param);  // MERGE 시도
        logger.info("[{}] SERVICE LOG : COPINO 저장/업데이트 완료", prefix);
    } catch (DuplicateKeyException e) {
        // DuplicateKeyException에 대한 예외처리로 UPDATE 시도
        logger.info("[{}] SERVICE LOG : 중복키로 인한 UPDATE 시도", prefix);
        copinoMapper.updateCopino(param);
    }
}

------------------------------------예외 처리(MERGE 재실행)-----------------

public void saveCopino(Map<String, Object> param) {
    try {
        copinoMapper.saveCopino(param);  // MERGE 시도
        logger.info("[{}] SERVICE LOG : COPINO 저장/업데이트 완료", prefix);
    } catch (DuplicateKeyException e) {
        // DuplicateKeyException에 대한 예외처리로 MERGE 재시도
        logger.info("[{}] SERVICE LOG : 중복키로 인한 MERGE 재시도", prefix);
        copinoMapper.saveCopino(param);  // 다시 MERGE
    }
}
```