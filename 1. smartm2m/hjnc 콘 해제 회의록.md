
- 게이트 진입 -> 전자 인수도증 발급 -> 블록 진입 -> (반입일 경우) 콘 해제 관련 안내 팝업 표출 -> 확인 버튼 클릭 -> 인수도증 화면 내 블록 내 작업 대기 차량 표기, 콘 해제 버튼 표기 -> 콘 해제 완료 버튼 클릭 -> 올컨e TOS로 정보 송신 -> TOS 정보 수신 -> 해당 차량 작업 순번 시 -> 콘 관련 안내 문구 표기 및 작업 중지 요청 버튼 활성화 -> 상차 시 -> 팝업 닫힘
- H : 블록 안에서 내려서 콘 해제 해야된다 무조건
- 팝업 문구는 터미널측에서 전달 예정
- H : 팝업에 대한 확인팝업은 자동으로 닫혀도 된다
- 블록 내 대기차량
	- 이건 순번이 아니다라는 정확한 안내를 줄거다
	- 앞차 뒤에 설 필요가 있는지 확인 하기 위함
	- HJNC는 블록 + 베이(수평) + 반입 반출 구분 까지 있어야된다..
- 반입일 경우만 블록 진입 시 콘 해제 버튼(팝업)을 띄울거다(활성화).
- 그럼에도 불구하고 X를 눌러서 꺼버리면 안되니까
	- 앞차 작업이 끝날때마다 팝업을 다시 띄운다.(백그라운드 상태면 TTS든 뭐든)
	- 그러면 블록에 잘못 들어왔을때도 팝업이 안뜨니 기사가 인지 가능하다
		- H : 타이밍은 
- 취소, 재전송 등 설계도 필요(그냥 냅다 콘해제 완료 버튼을 눌렀을 때를 방지, 실물과 데이터의 차이가 생기는 상황을 방지)
- 버튼이 깜박이는건 기본, 콘 해제 후 완료 버튼을 누르면 장비에 시그널을 주는것

- 게이트 들어오면 알림 문구 뜨고 -> 블록 진입 했을때 콘해제 하라고 팝업 뜨고 (계속 콘 해제 안하면 계속 알림 -> 버튼은 계속 깜박) -> 완료 버튼 누르면 작업 시작
	- 팝업은 없애고 바로 버튼만 깜박이게하고 -> 버튼 누르면 "작업 요청 하겠습니까?" 라는 문구를 확실히 해제 했냐는걸 확인하는듯한 문구로

- 블록엔터때는 작업 준비완료 버튼만 깜박깜박(팝업X) -> CPS 포지션 도착했고 크레인 프로세싱 상태인데도 버튼 눌렀다는 트리거가 안왔으면 -> 팝업 띄우기