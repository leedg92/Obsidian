# 그룹오더 START 타임아웃: 미팅 요지 (2026-09-18)

상세: `그룹오더-START-타임아웃-미팅자료-2026-09-16.md`(12.8절까지) / PPT `그룹오더-START-타임아웃-원인분석-v2.pptx`(12장)

## 1. 현상
- 천일 TMS가 90컨 이상 오더를 CREATE 하고 0.05초 뒤 START. 서버 10초 제한을 넘겨 408.
- 408을 받아도 처리는 끝까지 돌아 오더는 START 상태로 남고, 재생성하면 「컨테이너 중복」 거절.
- 30일 8건 = 천일형 6(98~133컨, 생성 직후 START) + 경진형 2(147·163컨, 겹침 없음).

## 2. 원인 (둘의 곱)
- 코드: 컨테이너마다 행을 많이 쓴다. START는 컨테이너당 3행, 생성 리스너는 컨테이너당 10왕복을 한 트랜잭션에.
- DB: 행당 14~16ms. 페이지 읽기 1회 2.35ms × 미스 5~7회. DB 183GB에 버퍼풀 32GB(17%). 같은 코드·같은 볼륨을 테스트베드에서 돌리면 행당 1.2ms.
- 1초 넘는 INSERT 857건은 예외 없이 랜덤 UUID 키 테이블. 그룹오더 밖에서도 한 행 INSERT가 1~2.5초.

## 3. START 10초 예산 (운영 로그 모델)
- 고정비 0.7초 + 컨테이너당 약 0.045초 + 블록체인 0.5초.
- 100컨 7.3초(리스너와 겹치면 9.6초) / 133컨 9.4초 / 147컨 10.1초.

## 4. 해결 방안
- 코드(컨테인어스)
  1. 생성 리스너 일괄화 + 트랜잭션 분할: 리스너 DB 점유 6.5 → 0.5초(12배). 겹침 꼬리 제거. START 자체는 안 줄어듦.
  2. UUIDv7 전환: 타입·스키마 그대로, 생성기 38곳만 교체. 행마다 나는 페이지 미스 제거. 코드 쪽에서 단가를 내리는 유일한 수단.
  3. START 조회 묶기(고정비 0.7 → 0.05초), 콜백매핑 조회 단순화.
  4. 비동기 접수 API 운영 배포: 10초 제한 자체가 없어짐. develop에 있고 운영 미배포.
- DB 설정(무중단): 버퍼풀 32GB → 메모리 여유만큼, io_capacity 200 → 2000, flush_neighbors 0, 슬로우 로그 ON.
- DB 인스턴스·인프라: 스토리지(페이지 읽기 2.35ms → 로컬 NVMe면 20배 감소), 메모리 증설, 183GB 다이어트.
- TMS(천일): CREATE 후 10초 텀. 천일형 완화, 147컨 이상은 못 막음.

## 5. A/B 테스트에서 말할 것
- 유효: 리스너 6.5 → 0.5초. 왕복 984 → 6회.
- 정정: 「코드만으로 START 절반」은 테스트베드 고유 현상. 검증행 FK가 마스터 행에 S 락을 걸고 START의 우선순위 UPDATE가 6.5초 기다린 것. 락을 빼고 다시 돌리면 A 7.67초 · B 7.80초로 같다.
- 운영은 진단 로그 631건에서 이 구간이 최대 18ms. 운영 START에 락 대기는 없다.
- 단, 운영에도 같은 FK가 있는데(131 확인) 왜 안 막히는지는 미확인. DBA에 확인 요청 한 줄:
  `SHOW GLOBAL VARIABLES LIKE 'foreign_key_checks'; SHOW GLOBAL VARIABLES LIKE 'tx_isolation';`

## 6. 예상 반문과 답
- "코드 고치면 천일형 사라지나?" → 완화. 100컨 9.6 → 6.6초, 133컨 9.4 → 8.8초라 여유 1초. 확실한 해결은 DB 단가 개선·UUIDv7·비동기 접수.
- "150컨도 되나?" → 코드만으로는 안 됨. 행당 16ms × 300행 = 4.8초가 START 안에 그대로 남는다.
- "테스트베드는 왜 빨랐나?" → 페이지 미스가 없어서. 지연 주입은 왕복만 흉내내고 배치 INSERT 안의 행당 비용은 못 만든다.
- "09-12 이후 0건이면 나은 것 아닌가?" → 천일의 100컨 이상 START가 1/4로 줄어서다(기대 0.6건). 개선 아님.

## 7. 한 줄 요약
코드의 낭비(왕복·중복 기록·랜덤 UUID)와 DB의 느린 단가(페이지 미스 × 2.35ms)가 곱해져 10초를 넘긴다. 리스너 수정은 DB 점유를 12배 줄이고, START 타임아웃 자체는 DB 단가와 UUIDv7이 맡는다. 코드는 컨테인어스가, 단가는 DB 쪽이 각자 맡아야 끝난다.
