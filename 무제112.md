# 외부 통신 하드코딩 URL 점검 결과

- 대상 리포지토리: `api-bctrans` (`/Users/idong-geun/IdeaProjects/api-bctrans`)
- 기준 브랜치/커밋: `develop` / `53c7edfa6` (pull 완료 시점)
- 점검일: 2026-09-16
- 빌드 프로필: `application-allcone.properties`, `application-bctrans.properties` (제우스 환경, 소스 빌드만 수행)
- 점검 방법: `src/main/java`, `src/main/resources` 전체에 대해 `http(s)://`, IP 리터럴, 도메인 리터럴, `@Value` 기본값, `WebClient/RestTemplate` base URL 리터럴 grep

---

## 1. 자바 소스에 하드코딩된 외부 URL (프로퍼티로 변경 불가)

| # | 파일 / 라인 | 하드코딩 값 | 용도 | 호출 조건 | 비고 |
|---|---|---|---|---|---|
| 1 | `src/main/java/.../gps/web/GpsTrackingController.java:70` | `https://accio.chainportal.co.kr/gps/api/gps/collect` | 앱에서 받은 GPS 포인트 배치를 클라우드 수집 서버로 바이패스 | `POST /mobile/gps/points` 호출 시, `collectYn == "Y"` 인 경우 | `collectYn` 도 같은 파일 40행에 `private final String collectYn = "Y"` 로 하드코딩되어 상시 ON |
| 2 | `src/main/java/.../bctrans/tss/service/PushTruckerService.java:56` | `https://hjtmsif.hanjin.co.kr/intf/odaRestService/grpOrdUnityRecv` | 한진 TMS 그룹오더 상태 전송 | `truckerId == "HJTML010"` | `IttTruckTransStatusUpdateService:69` → `pushTruckerService.send()` 경로로 호출 |
| 3 | `src/main/java/.../bctrans/tss/service/PushTruckerService.java:58` | `https://lets.lottegl.com/api/com/groupOrder/getGroupOrderCommon` | 롯데글로벌로지스 TMS 그룹오더 상태 전송 | `truckerId == "HDLOC050"` | 동일 |
| 4 | `src/main/java/.../bctrans/tss/service/PushTruckerService.java:60` | `https://SolApi.chunil.co.kr/htmls/Tss_Grp_AddCall.jsp` | 천일정기화물 TMS 그룹오더 상태 전송 | `truckerId == "CITLC051"` | 동일. 호스트명 대소문자 혼용(`SolApi`) |

> 위 4건은 운영 중 실제로 외부망으로 나가므로 **제우스 서버 아웃바운드 방화벽 허용 대상**입니다.
> 매칭되는 `truckerId` 가 없으면 URL 이 빈 값이라 호출하지 않고 경고 로그만 남깁니다.

---

## 2. `@Value` 기본값에만 URL이 박힌 경우 (프로퍼티로 오버라이드 가능 — 실제 문제 아님)

| # | 파일 / 라인 | 기본값 | 실제 적용 값 | 판정 |
|---|---|---|---|---|
| 1 | `src/main/java/.../common/holiday/client/PublicHolidayApiClient.java:43` | `https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService/getRestDeInfo` | `application.properties:28` 의 `holiday.api.base-url` 이 우선 적용 | 기본값 미사용, 문제 없음 |
| 2 | `src/main/java/.../allcone/auth/service/NiceAuthService.java:72` | 빈 문자열 | `application.properties:38` 의 `nice.api.base-url` | 문제 없음 |
| 3 | `src/main/java/.../bctrans/logExtractor/.../PeerLogExtractorAdapter.java:57` | 빈 문자열 | 프로필별 `log.peer-server.url` | 문제 없음 |

---

## 3. 프로퍼티로 관리되는 외부 통신 대상 (참고 — 방화벽/도메인 확인용)

### 3-1. 공통 (`application.properties`, 두 프로필 모두 로드)

| 키 | 값 | 용도 |
|---|---|---|
| `holiday.api.base-url` | `https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService/getRestDeInfo` | 공공데이터포털 공휴일(특일정보) 배치 |
| `nice.api.base-url` | `https://auth.niceid.co.kr` | NICE 통합인증(골디락스) |

### 3-2. `application-allcone.properties`

| 키 | 값 | 구분 |
|---|---|---|
| `fcm.url` | `https://fcm.googleapis.com/fcm/send` | 외부 |
| `bctransApi.url` | `https://bcapi.chainportal.co.kr/bctrans-api` | 자사 |
| `bctrans.url` | `https://bcapi.chainportal.co.kr/bctrans-api` | 자사 |
| `allcone.url` | `https://bcapi.chainportal.co.kr/allcone-api` | 자사 |
| `chainportal.main` | `https://www.chainportal.co.kr` | 자사 |
| `bc.url` | `https://openapi.chainportal.co.kr/api` | 자사 |
| `log.peer-server.url` | `http://192.168.100.59:8080/allcone-api` | 내부망 (peer 로그 추출, ALLCONE2) |

### 3-3. `application-bctrans.properties`

| 키 | 값 | 구분 |
|---|---|---|
| `fcm.url` | `https://fcm.googleapis.com/fcm/send` | 외부 |
| `bctransApi.url` | `https://bcapi.chainportal.co.kr/bctrans-api` | 자사 |
| `bctrans.url` | `https://bcapi.chainportal.co.kr/bctrans-api` | 자사 |
| `allcone.url` | `https://bcapi.chainportal.co.kr/allcone-api` | 자사 |
| `chainportal.main` | `https://www.chainportal.co.kr` | 자사 |
| `bc.url` | `https://openapi.chainportal.co.kr/api` | 자사 |
| `aas.url` | `https://tssapi.chainportal.co.kr/tss/aas/v1` | 자사 (TSS AAS) |
| `log.peer-server.url` | `http://192.168.100.59:8080/bctrans-api` | 내부망 (peer 로그 추출, BCTRANS2) |

---

## 4. 조치 제안

| 항목 | 조치안 | 우선순위 |
|---|---|---|
| GPS 바이패스 URL (`GpsTrackingController:70`) | `gps.cloud.collect-url` 프로퍼티로 분리, `collectYn` 도 `gps.collect-yn` 으로 외부화 | 높음 — 수집 서버 주소 변경 시 재빌드 필요 |
| 운송사 TMS URL 3건 (`PushTruckerService:56,58,60`) | `trucker.tms.url.<truckerId>` 형태 프로퍼티 맵으로 분리 (운송사 추가 시 코드 수정 불필요) | 높음 |
| 제우스 방화벽 | `accio.chainportal.co.kr`, `hjtmsif.hanjin.co.kr`, `lets.lottegl.com`, `SolApi.chunil.co.kr`, `fcm.googleapis.com`, `apis.data.go.kr`, `auth.niceid.co.kr` 아웃바운드 허용 확인 | 배포 전 필수 |
| `log.peer-server.url` | 제우스 실제 2호기 IP(`192.168.100.59`)가 맞는지 확인 후 프로필 값 갱신 | 중간 |

---

## 5. 점검 범위 밖 / 미발견

- `src/main/java` 내 외부 도메인 문자열 리터럴: 위 표 외 없음
- `src/main/java` 내 IP 리터럴: `127.0.0.1` (필터 fallback), Swagger `@Schema(example=...)` 용 `192.168.1.1` 뿐 — 실제 통신 아님
- `WebClient.create(...)`, `baseUrl(...)`, `setRootUri(...)` 에 박힌 외부 URL: 없음
- `src/test` 는 점검 대상에서 제외 (war 미포함)
