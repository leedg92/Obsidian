# 금주 작업 공수 산정 (2026-07-27 주)

> - 단위: MD(man-day, 1인 기준). 순수 개발시간 기준.
> - 외부(터미널·관제·앱) 응답 대기시간은 제외.
> - **작업 브랜치는 모두 `develop`에서 분기.**

## 요약

| #   | 항목                                 | 공수        |
| --- | ---------------------------------- | --------- |
| 1   | HPNT/PNIT 차상세척 기능 테스트              | 0.5       |
| 2   | 관제시스템 메인 공지팝업 연계                   | 0.3       |
| 3   | 트럭코드 변경                            | 0.2       |
| 4   | BPT/HKTG 인수도증 취소 조건 수정             | 0.1       |
| 5   | HPNT 차상세척 미지원 시간대 차단               | 1.0       |
| 6   | 차량등록증·대표차량 인증·인가 보완 (ACE-FUN-011)  | 0.5       |
| 7   | 공컨교체·도어방향전환·차상세척 취소 API 3종         | 1.5       |
| 8   | 인근 도로 혼잡도 조회 (조사·설계)               | 0.5       |
| 9   | 이용약관 페이지 + 관제 연계                   | 0.8       |
| 10  | 그룹오더 컨테이너 진행상태 조회 (BPABCTRANS-238) | 1.0       |
|     | **합계**                             | **6.4MD** |

**1주(5MD) 초과.** 최소 1.4MD를 다음 주로 빼야 한다. 후보는 8(출처 미상이라 어차피 불확실) 또는 9(동의 이력 범위 미정).

## 권장 착수 순서

1. **주 초반에 5·8을 먼저 찔러본다.** 외부 의존이 걸린 유일한 두 건이라, 막히는 게 확인되면 조정 여지가 있을 때 빼야 함. 목요일에 터지면 대응 불가.
2. 단타 4건(2·3·4·1)을 연속 처리 — 합쳐서 1.1MD.
3. 6 → 10 → 7 → 9 순으로 마무리.

---

## 항목별 상세

### 1. HPNT/PNIT 차상세척 기능 테스트 — 0.5MD

- 개발은 이미 완료된 상태. 순수 연동 테스트 공수.
- 관련 코드: `ContainerCleaningRequestDto`, `TerminalContainerWashRequestDto`, `ContainerHandlerRequestController`
- 테스트 성격상 압축이 어려운 항목. 터미널 측 응답 대기시간은 별도.

### 2. 관제시스템 메인 공지팝업 연계 — 0.3MD

- **수신 API 1개만 신설.** 받아서 `bctransdbx.popup` 테이블에 INSERT.
- 앱 조회 API는 **이미 존재** → `GET /mobile/popup/list` (`PopupController`). 신규 개발 불필요.
- 팝업 테이블도 기존 것 재사용.
- ※ BPABCTRANS-244(점검페이지 연계)와는 **별개 기능**. 혼동 주의.

### 3. 트럭코드 변경 — 0.2MD

- 기존 `POST /mobile/user/truck/updateTruckCode` 그대로 사용. 현재 미사용 상태로 보임.
- **스키마 변경 불필요**: `user_regist_truck` PK가 `(TRUCK_NUMBER, USER_ID)` 복합키라 트럭번호 1개에 유저 N명 → 행 N개 → `TRUCK_CODE`가 유저별로 각각 저장 가능. 요구사항(트럭번호 1 : 트럭코드 N)이 이미 충족됨.
- `updateUserTruckCode` UPDATE문도 WHERE가 PK 전체 매칭이라 정확히 1건만 갱신. 문제 없음.
- 부수 정리: `UserTruckMapper.xml`의 `selectUserTrucks`에 `IFNULL(TRUCK_CODE,'') AS truckCode`가 **2번 중복**(14~15줄). **develop에도 동일하게 존재.** 동작엔 무해(뒤 값이 먹음)하나 이번에 제거.
- **확인 필요**: 유저별로 트럭코드가 다를 때, 실제 오더에 실리는 `truckCode`가 어느 행 것인지(대표차량 소유자 기준으로 추정되나 미확인).

### 4. BPT/HKTG 인수도증 취소 조건 수정 — 0.1MD

- 지정된 코드블럭 삭제만 하면 됨. 회귀 확인 포함.
- 대상 코드블럭은 별도 전달 예정.

### 5. HPNT 차상세척 미지원 시간대 차단 — 1.0MD

**적용 규칙 (HPNT만. PNIT는 제한 없음)**

| 요일 | 운영시간 | 마지막 요청 가능 |
|---|---|---|
| 평일 | 08:30 ~ 17:00 | 16:55 |
| 토요일 | 08:30 ~ 15:00 | 14:55 |
| 일요일 | 불가 | - |
| 공휴일(평일 포함) | 불가 | - |

**구현 방식**

- 요일별 시간표: **설정 테이블 1개**로 관리(요일·시작·종료·마감컷). 공공데이터와 무관하므로 분리.
- 공휴일: **공공데이터 API를 월 1회 배치로 DB 적재.** 요청 경로에서 매번 호출하지 않음.
  - 사유: 요청 시점에 외부 API를 타면 포털 장애·지연이 곧바로 차상세척 판정 불가로 전파됨. 공휴일은 연 단위로 거의 확정된 데이터라 실시간성이 불필요.
  - 배치 실패 대비: 적재 시 향후 3개월치를 함께 넣어 공백 방지 + 임시공휴일 수동 등록 가능한 구조.
- 차단 위치: **서버 거절이 정본, 앱 버튼 비활성은 UX 보조**로 이중화.
  - 사유: 앱만 막으면 구버전 앱이 그대로 통과함.

**공수 내역**: DDL(공휴일·시간표 테이블) + 월배치 + 판정로직 + 서버 거절 + 앱 응답 처리

### 6. 차량등록증·대표차량 인증·인가 보완 (ACE-FUN-011) — 0.5MD

**현재 문제**

| 엔드포인트 | 상태 | 판단 |
|---|---|---|
| `GET /mobile/user/truck/certificate` | `@RequestHeader("deviceToken")` **없음**. `userId`+`truckNo`를 파라미터로 직접 수신 | **문제.** 남의 userId+차량번호를 넣으면 타인 차량등록증 이미지가 그대로 조회됨 (개인정보 노출) |
| `POST /mobile/user/truck/updateTruckCode` | 인증 헤더 없음 | **문제.** 타인 트럭코드 변경 가능. 앱 개발자 지적에는 없으나 같이 처리 |
| `POST /mobile/user/truck/rep/manual` | 인증 없음 (내부 운영용) | 외부에서 호출 가능. 최소 IP 제한 필요 |
| `GET /`, `/duplicate`, `/delete` 등 | deviceToken을 받지만 `ObjectUtils.isEmpty()` 검사만 하고, 조회 주체는 파라미터 `userId`를 신뢰 | **문제.** 임의 문자열 토큰 + 남의 userId로 타인 차량 조회·조작 가능 |
| `GET /mobile/user/truck/rep/v2` | truckNo를 앱이 전달 | **문제 아님.** 등록 전 "이 차량이 남의 대표차량인가" 중복확인 용도라 타인 차량번호 입력이 정상 동작 |

**수정 방향**

- `deviceTokenService.getUserIdByDeviceTokenV2(deviceToken)` 로 **서버가 호출자를 판정**하고, 파라미터 `userId`는 무시.
  - 해당 메서드는 이미 존재하며 `checkRepTruckV2`에서 사용 중 → **신규 설계 없이 기계적 치환.** 0.5MD로 잡은 근거.
  - 부수 효과로 **토큰 유효성 검증도 자동 해결.** 현재는 "비었는지"만 보고 실제 등록된 토큰인지 확인하지 않는데, 역조회하면 미등록 토큰은 null로 걸러짐.
- 어드민 대행 경로(`TempAdminUserTruckService`)는 예외 유지.

**앱 수정 동반 여부**

- `certificate`, `updateTruckCode` → 앱이 deviceToken 헤더를 새로 실어야 함 (앱 수정 필요)
- 그 외 → 서버 단독 수정으로 완결 (앱 무영향)
- → 서버 수정을 먼저 올리고 앱 반영은 앱 배포 일정에 맞추면 블로킹되지 않음.

### 7. 공컨교체·도어방향전환·차상세척 취소 API 3종 — 1.5MD

- **3종이 동일 패턴**(`TerminalRequestService` 경로)이라 첫 개를 뚫으면 나머지는 사유코드·엔드포인트 교체 수준. 이것이 1.5MD로 잡은 근거.
- 터미널 전송은 **플레이스홀더로 먼저 구현**하고, 터미널 측 인터페이스는 협의하며 맞춰가는 방식.
- **취소 가능 타이밍 설계가 실질 리스크** — 어느 상태까지 취소를 허용할 것인가. 코딩보다 결정이 오래 걸림.
- 참고: 도어방향전환 TSS 확장 계획은 `HPNT_tss_changedoor_plan.md`에 기 작성됨.

### 8. 인근 도로 혼잡도 조회 (조사·설계) — 0.5MD

- **데이터 출처 파악부터** 필요(외부 공공 API 여부, 갱신 주기, 좌표 매핑 방식).
- 0.5MD는 **"출처가 바로 나온다"는 가정값.** 구현 공수는 출처 확정 후 별도 산정.

### 9. 이용약관 페이지 + 관제시스템 연계 — 0.8MD

- 관제(관리페이지)에서 약관 등록 → bctrans 저장 → 앱에 **HTML 내려주는 방식.** 사실상 CRUD.
- 동의 이력 저장 범위 미정. **포함 시 +0.5MD** (이력 테이블 + 동의 API + 버전 관리).

### 10. 그룹오더 컨테이너 진행상태 조회 (BPABCTRANS-238) — 1.0MD

- `dispatchGroup` 수신 → 그룹오더 소속 컨테이너를 작업전/중/완료로 분류 반환.
- **쿼리 설계는 2026-07-16에 이미 확정** (`.review-context/decisions.json` task `group-order-status-api`, D001~D006). 브랜치 미생성·구현 미착수.
- 기존 `getGroupConList`/`getGroupTruckList`(`TssGroupOrderMapper.xml`)와 소스가 같아 **분류 컬럼 추가 수준.** 신규 조인 설계 없음.
- 구현 순서: 매퍼 → DTO → 서비스 조립 → 컨트롤러(`bctrans/group/web/TssGroupOrderController.java`)
- **착수 전 미결 2건(D005·D006) 확정 필요.**
- 주의: 미진입 차량 추출 시 "`_c`에 TRUCK_NO가 없는 차량"으로 잡으면 안 됨 — nextJob으로 연속 배차되므로 1건 완료 후 대기 중인 차량이 누락됨. `not exists`로 추출.

---

## 리스크

합계 6.4MD로 **1주(5MD)를 초과한다.** 아래 3건이 추가 변수다.

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| 1 | **공공데이터 API 신규 연동** (항목 5) | 인증키 발급·스펙 확인·응답 포맷 정합에서 반나절 단위로 샘 | 주 초반 착수해 조기 확인 |
| 2 | **혼잡도 데이터 출처 미상** (항목 8) | 쓸 만한 공공 API가 없으면 설계 자체가 멈춤. 공수가 아니라 미지수 | 주 초반 조사. 막히면 이 항목만 다음 주로 분리 |
| 3 | **취소 타이밍 결정 왕복** (항목 7) | 코딩은 빠른데 상태 허용범위 결정에서 하루 소요 가능 | 착수 전 결정 확정 |

---

## 미결 / 확인 필요 사항

| # | 관련 항목 | 내용 |
|---|---|---|
| 1 | 3 | 유저별 트럭코드가 다를 때 오더에 실리는 `truckCode`의 기준 행 확인 |
| 2 | 4 | 삭제 대상 코드블럭 전달 대기 |
| 3 | 6 | `certificate`·`updateTruckCode` 헤더 추가 관련 앱 개발자 협의 |
| 4 | 7 | 터미널 측 취소 인터페이스 협의 개시 시점 / 취소 허용 상태 범위 |
| 5 | 8 | 혼잡도 데이터 출처 |
| 6 | 9 | 동의 이력 저장 포함 여부 |
| 7 | 10 | 미결 2건(D005·D006) 확정 — `.review-context/decisions.json` |
