# 주간보고 통계 · 회원집계 API

## 엔드포인트 목록

|   |   |   |   |
|---|---|---|---|
|#|메서드|경로|파라미터|
|1|GET|`/member-stat/v1/monthly`|`yyyymm` (필수, `202607` 또는 `2026-07`) · `baseDate` (선택, `2026-08-01`)|
|2|GET|`/member-stat/v1/weekly`|`startDate`·`endDate` (필수, `2026-07-25` / 최대 7일)|
|3|GET|`/weekly-report/v1/eir-vbs/daily`|`startDate`·`endDate` (필수, `20260701`)|
|4|GET|`/weekly-report/v1/eir-vbs/monthly`|`yyyymm` (필수, `202607`)|
|5|GET|`/weekly-report/v1/eir-vbs/cumulative`|`year` (필수, `2026`)|
|6|GET|`/weekly-report/v1/usage-stat/daily`|`startDate`·`endDate` (필수, `20260701`)|
|7|GET|`/weekly-report/v1/usage-stat/monthly`|`yyyymm` (필수, `202607`)|
|8|GET|`/weekly-report/v1/usage-stat/cumulative`|`todayDate` (필수, `20260731`)|
|9|GET|`/weekly-report/v1/openapi/ent-monthly`|`yyyymm` (필수, `202607`)|
|10|GET|`/weekly-report/v1/openapi/ent-cumulative`|`todayDate` (필수, `20260731`)|
|11|GET|`/weekly-report/v1/openapi/api-monthly`|`yyyymm` (필수, `202607`)|
|12|GET|`/weekly-report/v1/openapi/api-cumulative`|`todayDate` (필수, `20260731`)|

> 날짜 형식이 다름 — **회원집계(1·2)는 `yyyy-MM-dd`**, 주간보고(3~12)는 `yyyyMMdd`.

---

## 1. `GET /member-stat/v1/monthly`

```jsonc
{
  "result": "success",              -- 처리 결과
  "resultMessage": "",              -- 실패 시 메시지
  "data": {
    "statMonth": "2026-07",         -- 통계 대상 월 (요청이 202607이어도 응답은 yyyy-MM)
    "baseDate": "2026-08-01",       -- 활성 누계 기준 시점 (생략 시 조회월 다음달 1일)
    "driverJoinCnt": 150,           -- 그 달에 가입한 운송기사 (이후 탈퇴자 포함)
    "driverUnusedCnt": 7,           -- 그 달에 탈퇴한 운송기사 (가입 시점 무관)
    "driverActiveJoinCnt": 147,     -- 그 달에 가입해 현재까지 유지 중인 기사
    "driverActiveTotalCnt": 16864,  -- baseDate 시점 활성 기사 누계 (탈퇴자 제외)
    "userJoinCnt": 219,             -- 그 달에 가입한 일반회원
    "companyJoinCnt": 21,           -- 그 달에 가입한 업체 (일반업체·화주·터미널·운송사·환적화주)
    "bpaJoinCnt": 11,               -- 그 달에 가입한 BPA 직원
    "totalMemberCnt": 24491         -- 누적 회원 (운송기사 제외 / 탈퇴자 포함)
  }
}
```

## 2. `GET /member-stat/v1/weekly`

```jsonc
{
  "result": "success",
  "resultMessage": "",
  "data": [
    {
      "statDate": "2026-07-31",       -- 통계 일자
      "driverJoinCnt": 6,             -- 그날 가입한 운송기사
      "driverUnusedCnt": 0,           -- 그날 탈퇴한 운송기사
      "driverActiveJoinCnt": 6,       -- 그날 가입해 현재까지 유지 중인 기사
      "driverActiveTotalCnt": 16864,  -- 그날 자정 기준 활성 기사 누계 (탈퇴자 제외)
      "userJoinCnt": 7,               -- 그날 가입한 일반회원
      "companyJoinCnt": 0,            -- 그날 가입한 업체
      "bpaJoinCnt": 0,                -- 그날 가입한 BPA 직원
      "totalMemberCnt": 24491         -- 그날 자정 기준 누적 회원 (운송기사 제외 / 탈퇴자 포함)
    }
  ]
}
```

---

## 주간보고 공통 응답 봉투 (3~12 전부 동일)

```jsonc
{
  "httpStatus": 200,        -- 처리 상태 (오류도 HTTP 200으로 내려옴)
  "success": true,          -- 성공 여부
  "data": ...,              -- 실제 데이터 (API별로 배열 또는 객체)
  "errorTitle": null,       -- 오류 제목
  "errorMessage": null,     -- 오류 내용
  "errorFlag": null,        -- 오류 플래그 (boolean 아닌 String)
  "alert": false            -- 알럿 표시 여부
}
```

아래는 각 API의 `data` 부분만 기술한다.

## 3. `GET /weekly-report/v1/eir-vbs/daily`

```jsonc
"data": {
  "daily": [
    {
      "statDate": "2026-07-01",   -- 통계 일자
      "isFinal": "Y",             -- 확정 여부
      "eirCnt": 33377,            -- 전자인수도증 건수
      "eirRate": 97.51,           -- 전자인수도증 비율(%)
      "vbsNotReserved": 39437,    -- VBS 미예약
      "vbsCompliant": 109,        -- VBS 준수
      "vbsNonCompliant": 213      -- VBS 미준수
    }
  ],
  "total": {                      -- 조회 기간 합계 (일자별과 같은 구성, statDate·isFinal 없음)
    "eirCnt": 67875,
    "eirRate": 97.49,
    "vbsNotReserved": 80174,
    "vbsCompliant": 211,
    "vbsNonCompliant": 407
  }
}
```

## 4. `GET /weekly-report/v1/eir-vbs/monthly`

```jsonc
"data": [
  {
    "statMonth": "202607",       -- 통계 대상 월
    "eirCnt": 842849,            -- 전자인수도증 건수
    "eirRate": 97.27,            -- 전자인수도증 비율(%)
    "vbsNotReserved": 994721,    -- VBS 미예약
    "vbsCompliant": 2907,        -- VBS 준수
    "vbsNonCompliant": 5694      -- VBS 미준수
  }
]
```

## 5. `GET /weekly-report/v1/eir-vbs/cumulative`

```jsonc
"data": {                      -- 배열이 아닌 객체 1개
  "eirCnt": 5728208,           -- 연간 누계 전자인수도증 건수
  "eirRate": 94.09,            -- 연간 누계 전자인수도증 비율(%)
  "vbsNotReserved": 6509363,   -- 연간 누계 VBS 미예약
  "vbsCompliant": 25303,       -- 연간 누계 VBS 준수
  "vbsNonCompliant": 44017     -- 연간 누계 VBS 미준수
}
```

## 6. `GET /weekly-report/v1/usage-stat/daily`

```jsonc
"data": {
  "daily": [
    {
      "sysId": "10",                    -- 시스템 코드
      "sysNm": "1. 항만트럭예약 시스템",  -- 시스템 이름 (앞 번호가 이름에 포함됨)
      "useDt": "2026-07-01",            -- 이용 일자
      "userCnt": 2                      -- 이용자 수
    }
  ],
  "total": [                            -- 시스템별 기간 합계 (useDt 없음)
    {
      "sysId": "10",
      "sysNm": "1. 항만트럭예약 시스템",
      "userCnt": 6
    }
  ]
}
```

## 7. `GET /weekly-report/v1/usage-stat/monthly`

```jsonc
"data": [
  {
    "sysId": "10",                    -- 시스템 코드
    "sysNm": "1. 항만트럭예약 시스템",  -- 시스템 이름
    "useDt": "202607",                -- 이용 월
    "userCnt": 109                    -- 이용자 수
  }
]
```

## 8. `GET /weekly-report/v1/usage-stat/cumulative`

```jsonc
"data": [
  {
    "sysId": "10",                    -- 시스템 코드
    "sysNm": "1. 항만트럭예약 시스템",  -- 시스템 이름
    "userCnt": 1226                   -- 누계 이용자 수
  }
]
```

## 9. `GET /weekly-report/v1/openapi/ent-monthly`

```jsonc
"data": [
  {
    "entCd": "ENT0000001",         -- 기관 코드
    "entNm": "부산항만공사",         -- 기관 이름
    "regApi": "IF-ITT-TIS-019",    -- API 코드 (값이 "ALL"이면 그 기관의 총합계 행)
    "apiNm": "공지사항조회",         -- API 이름
    "totalCount": 137372,          -- 호출 건수
    "datea": "202607"              -- 대상 월 (체인포털 원본 필드명)
  }
]
```

## 10. `GET /weekly-report/v1/openapi/ent-cumulative`

```jsonc
"data": [
  {
    "entCd": "ENT0000001",         -- 기관 코드
    "entNm": "부산항만공사",         -- 기관 이름
    "regApi": "IF-ITT-TIS-019",    -- API 코드 (값이 "ALL"이면 그 기관의 총합계 행)
    "apiNm": "공지사항조회",         -- API 이름
    "totalCount": 747203           -- 누계 호출 건수
  }
]
```

## 11. `GET /weekly-report/v1/openapi/api-monthly`

```jsonc
"data": [
  {
    "regApi": "IF-ITT-TIS-001",    -- API 코드
    "apiNm": "선석배정현황조회",     -- API 이름
    "totalCount": 268409,          -- 호출 건수
    "datea": "202607"              -- 대상 월 (체인포털 원본 필드명)
  }
]
```

## 12. `GET /weekly-report/v1/openapi/api-cumulative`

```jsonc
"data": [
  {
    "regApi": "IF-ITT-TIS-001",    -- API 코드
    "apiNm": "선석배정현황조회",     -- API 이름
    "totalCount": 1478358          -- 누계 호출 건수
  }
]
```