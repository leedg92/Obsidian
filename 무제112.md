## 흐름 ①  등록

|    #    | 시나리오  | 관제                                     | 올컨e 서버                                            | 앱                          |
| :-----: | ----- | -------------------------------------- | ------------------------------------------------- | -------------------------- |
| **T01** | 목록 조회 | 화면 진입. 5종이 순서대로 뜨고 버전·필수·시행일이 보이는가     | `GET /terms/list` 200. 정렬이 **필수 우선 → sortOrder**  | —                          |
| **T02** | 신규 등록 | `TEST_TERMS` 등록 (필수 Y · 시행일 오늘 · 순서 9) | `termsId` 채번 · `version=1`. 로그 `새 약관 종류 등록`       | `GET /mobile/terms` 목록에 뜬다 |
| **T03** | 앱 동의  | —                                      | `allcone_terms_agreement` 에 행 생성 (`SOURCE='APP'`) | 동의 후 재조회 시 `agreeYn='Y'`   |

---

## 흐름 ②  개정

|    #    | 시나리오      | 관제                         | 올컨e 서버                                                                                       | 앱               |
| :-----: | --------- | -------------------------- | -------------------------------------------------------------------------------------------- | --------------- |
| **T04** | 개정 발행     | 같은 종류에 본문만 바꿔 발행           | `version=2` · **termsId 새로 채번**. 🔴 `allcone_terms_type` 의 `SORT_ORDER`·`USE_YN` 이 **그대로인지** | 앱 목록이 v2 로 바뀐다  |
| **T05** | 개정 후 재동의  | —                          | 로그인 응답 `needsTermsAgreement=true`                                                            | 재동의 화면이 뜬다      |
| **T06** | 같은 내용 재전송 | T04 와 **완전히 같은 값**으로 한 번 더 | 🔴 **새 버전이 생기면 안 된다.** 기존 termsId·version 을 그대로 반환 + 로그 `직전 버전과 동일해 재발행하지 않는다`               | 변화 없음           |
| **T07** | 시행 예정일 발행 | 시행일을 **내일**로 두고 발행         | 현행은 v2 유지, `scheduledVersion` 에 새 버전이 잡힌다                                                    | **아직 안 나와야** 한다 |

```sql
SELECT TERMS_TYPE, SORT_ORDER, USE_YN FROM allcone_terms_type WHERE TERMS_TYPE='TEST_TERMS';
```

---

## 흐름 ③  노출 · 순서

| # | 시나리오 | 관제 | 올컨e 서버 | 앱 |
|:--:|---|---|---|---|
| **T08** | 노출 끄기 → 켜기 | 노출을 N 으로 했다가 다시 Y | `USE_YN` 만 바뀌고 버전·동의 이력은 그대로 | N 이면 **목록에서 사라지고**, Y 면 다시 뜬다 |
| **T09** | 내려둔 약관 개정 | 노출 N 상태에서 개정 발행 | 🔴 **`USE_YN` 이 'N' 을 유지하는지.** 'Y' 로 되돌아오면 관제가 useYn 을 안 보낸 것 | 내려간 약관이 다시 뜨면 안 된다 |
| **T10** | 순서 변경 | 목록에서 순서를 바꿔 저장 | `POST /terms/sort` 응답 `updated` = 보낸 건수. **보내지 않은 종류는 그대로** | 표출 순서가 따라 바뀐다 (필수 우선은 유지) |

---

## 흐름 ④  취소

| # | 시나리오 | 관제 | 올컨e 서버 | 앱 |
|:--:|---|---|---|---|
| **T11** | 동의 있는 버전 취소 | T03 에서 동의한 버전을 취소 시도 | 🔴 **거절.** `동의 이력이 있는 약관 버전은 취소할 수 없습니다 … 동의 N건` → 화면에 그대로 보이는가 | — |
| **T12** | 동의 0건 버전 취소 | 아무도 동의하지 않은 최신 버전을 취소 | `DATA_STATUS='DATA_DELETED'`. 현행 버전이 **직전 버전으로 되돌아간다** | 앱 목록도 직전 버전으로 |
| **T13** | 이력 조회 | 종류를 열어 버전 이력 확인 | **취소된 버전이 이력에 나오는지** — 노출 여부를 관제와 합의 | — |

> 정정이 폐지돼 **발행을 되돌리는 수단은 CANCEL 하나뿐이고, 그마저 동의 0건일 때만** 된다.
> 한 명이라도 동의하면 개정으로 새 버전을 올리는 수밖에 없다(= 전원 재동의). 관제가 이 제약을 이해했는지 확인하는 자리다.
