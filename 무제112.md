# 작업 기록 (2026-07-28)

> 리포: `api-bctrans-legacy-analyze` (`bpa/allcone/prod/api-bctrans.git`)
> 기준 문서: `금주-작업-공수산정.md` — 이 기록은 그 목록의 진행 상황이다.

## 요약

| 항목                               | 상태                                                      |
| -------------------------------- | ------------------------------------------------------- |
| develop에 BPABCTRANS-270 반영       | ✅ squash 머지 + origin push                               |
| BPABCTRANS-261 트럭코드 수정 API 인증    | ✅ 구현·머지·push (테스트 미실행)                                  |
| BPABCTRANS-264 ACE-FUN-011 인증 보완 | 🔶 `GET /certificate` 1건 완료·develop 머지·push. 나머지 7건 미착수 |
| BPABCTRANS-159 관제 메인 공지팝업 연계     | 🔶 구현·머지·push 완료. **DDL 미적용 / 컴파일·테스트 미실행**             |

---

## 1. develop 정비 — BPABCTRANS-270

FCM 전체 발송 제거 작업(`feature/BPABCTRANS-270-removeFCMSendForAll`)을 develop에 반영했다.

- 최초 `--no-ff` 머지커밋(`9004dfab`)으로 넣었다가, **squash 방식으로 재처리** (`git reset --hard origin/develop` 후 `merge --squash`)
- 결과 커밋: **`4c210f64`** "전체 토큰 대상 FCM 발송 제거 (지오펜스 sync / 점검페이지 연계)"
- `origin/develop` push 완료 (`3efa2fec..4c210f64`, fast-forward)
- 검증: `git diff feature/BPABCTRANS-270-removeFCMSendForAll develop` → 차이 0

변경 파일 3개 (`GeofenceSyncService`, `GeofenceSyncController`, `MaintenanceService`), +9 / -64.

---

## 2. BPABCTRANS-261 — 트럭코드 수정 API 인증 추가

**브랜치**: `feature/BPABCTRANS-261-modifyTruckCode` (develop `4c210f64`에서 분기)
**커밋**: `71e9e43b` → develop에 squash 머지 **`f20252e5`** → origin push 완료

### 문제

`POST /mobile/user/truck/updateTruckCode`가 인증 없이 열려 있었다. body의 `userId` + `truckNo`를 그대로 신뢰해서, **타인의 userId를 넣으면 남의 트럭코드를 변경할 수 있었다.**

### 수정 — `UserTruckController.java:293`

```java
@PostMapping("/updateTruckCode")
public ApiResponse updateUserTruckCode(
        @RequestHeader("deviceToken") String deviceToken,      // 추가
        @RequestBody UpdateUserTruckCodeDTO updateUserTruckCode
) {
    if (ObjectUtils.isEmpty(deviceToken)) {
        return ApiResponse.error("디바이스 토큰을 확인할 수 없습니다.");
    }

    String tokenUserId = deviceTokenService.getUserIdByDeviceTokenV2(deviceToken);
    if (ObjectUtils.isEmpty(tokenUserId)) {                    // 미등록/만료 토큰 → null
        return ApiResponse.error("디바이스 토큰을 확인할 수 없습니다.");
    }

    if (!tokenUserId.equals(updateUserTruckCode.getUserId())) {
        return ApiResponse.error("본인 차량만 수정할 수 있습니다.");
    }
    ...  // 이하 기존 로직 그대로
}
```

방어선 3단: **헤더 누락 → 미등록/만료 토큰 → 타인 userId**.
부수 효과로 토큰 유효성 검증도 해결된다. 기존에는 `isEmpty()`만 봐서 임의 문자열이 통과했으나, `getUserIdByDeviceTokenV2`로 역조회하면 미등록 토큰은 `null`로 걸러진다.

### 결정 사항

| 논점 | 결정 | 근거 |
|---|---|---|
| 인증 방식 | **body userId와 비교 후 불일치면 거절** (토큰 userId로 덮어쓰기 아님) | 사용자 지시 |
| 어드민 대행(`TempAdminUserTruckService`) 예외 | **두지 않음** | 트럭코드 수정은 운영 대행 수요가 없음. 우회로를 안 만드는 쪽 |
| 0건 갱신 체크 | **넣지 않음** | 인증이 붙으면 타인 userId는 앞에서 걸림. 남는 건 stale truckNo 정도라 실익 없음 |
| 쿼리 변경 | **없음** | `UserTruckMapper.xml:71`이 이미 받은 값을 그대로 UPDATE |

### 검증 상태

- ✅ 컴파일 통과 (`mvn -q compile`, exit 0)
- ❌ **테스트 미실행** — 사용자가 직접 수행 예정

### 손대지 않은 것 (승인 대기)

| 건 | 위치 | 내용 |
|---|---|---|
| 예외 로그 추가 | `UserTruckService.java:145` | `catch (Exception e) { return false; }` — 로그가 없어 실패 원인 추적 불가. `logger.error` 1줄 추가 제안했으나 판단 미수령 |
| 중복 컬럼 제거 | `UserTruckMapper.xml:14~15` | `IFNULL(TRUCK_CODE,'') AS truckCode`가 2번 중복. develop에도 동일 존재. 동작은 무해 |

---

## 3. BPABCTRANS-264 — ACE-FUN-011 인증 보완 (1/8 완료)

**브랜치**: `feature/BPABCTRANS-264-appToAllconeDeviceToken` (develop `f20252e5`에서 분기)
**커밋**: `da9cf7c1` → origin push 완료 → develop에 squash 머지 **`8ffd6189`** → origin push 완료
**남은 7건(2~8번)은 이 브랜치에서 이어서 진행 가능** (develop 머지는 1건 단위로 이미 반영됨)

### 진단서 원문 (ACE-FUN-011 — 차량등록증 및 대표차량 조회)

> - API 호출 시 userId, deviceToken 등 호출자 식별 정보가 전달되지 않음
> - 서버에서 호출자 식별 및 차량 소유권 검증이 불가능한 구조
> - 앱에서 대표차량번호를 직접 전달하는 방식으로 동작
> - 인증·인가 방식 및 소유권 검증 로직 보완 필요 **또는 그냥 둬도 되는 것인지 확인 필요**

**두 개의 다른 API를 묶어 지적한 것으로 판단된다.**

| 진단서 문구 | 해당 API | 판단 |
|---|---|---|
| "차량등록증 조회" / "호출자 식별 정보 없음" | `GET /certificate`(144) | 실제 문제. **이번에 조치 완료** |
| "**앱에서 대표차량번호를 직접 전달**하는 방식으로 동작" | `GET /rep/v2`(153) | 현재형 서술이고 앱 동작과 일치. 아래 판단 참조 |

`GET /certificate`는 앱에 remote config 키조차 없어 **호출 자체가 불가능**하다. 즉 진단자가 앱 동작을 보고 쓴 문장은 `rep/v2` 쪽이고, `certificate`는 API 명세/서버 코드를 보고 지적한 것으로 보인다.

### 완료 — `GET /certificate` 인증 추가 (`UserTruckController.java:144`)

```java
@GetMapping(value = "/certificate")
public void getCertImage(
        @RequestHeader(value = "deviceToken", required = false) String deviceToken,  // 추가
        @RequestParam String userId,
        @RequestParam String truckNo,
        HttpServletResponse response
) {
    if (ObjectUtils.isEmpty(deviceToken)) {
        response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);   // 401
        return;
    }

    String tokenUserId = deviceTokenService.getUserIdByDeviceTokenV2(deviceToken);
    if (ObjectUtils.isEmpty(tokenUserId)) {
        response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);   // 401
        return;
    }

    if (!tokenUserId.equals(userId)) {
        logger.warn("[...] 타인 차량등록증 조회 시도: tokenUserId={}, 요청 userId={}, truckNo={}", ...);
        response.setStatus(HttpServletResponse.SC_FORBIDDEN);      // 403
        return;
    }

    userTruckService.getTruckCertImage(userId, truckNo, response);
}
```

**261과 다르게 처리한 점**

| 항목 | 처리 | 이유 |
|---|---|---|
| 실패 응답 | `ApiResponse`가 아닌 **HTTP 상태코드**(401/403) | 반환형이 `void`이고 이미지 바이너리를 직접 스트림으로 씀 |
| `@RequestHeader` | `required = false` + 직접 검사 | 필수로 두면 헤더 누락 시 Spring이 400을 던짐. 인증 실패는 401이 맞음 |
| 타인 조회 시도 | `logger.warn` 기록 | 개인정보 노출 시도라 흔적을 남김 |

- ✅ 컴파일 통과 / ❌ 테스트 미실행

### 남은 대상 — `UserTruckController`

| # | 엔드포인트 | 라인 | 상태 |
|---|---|---|---|
| ~~1~~ | ~~`GET /certificate`~~ | ~~144~~ | ✅ **완료** (`da9cf7c1`) |
| 2 | `GET /` | 46 | 토큰 `isEmpty()`만 검사, 주체는 파라미터 `userId` 신뢰 |
| 3 | `GET /duplicate` | 60 | 〃 |
| 4 | `POST /` | 88 | 〃 (`request.getParameter("userId")`) |
| 5 | `POST /certificate` | 111 | 〃 |
| 6 | `POST /delete` | 254 | 〃 |
| 7 | `GET /rep/canceluser` | 268 | 〃 |
| 8 | `GET /rep/check` | 281 | 〃 |
| 9 | `POST /rep/manual` | 324 | 인증 없음 (내부 운영용) — IP 제한 필요, 범위 제외 |

### `GET /rep/v2`(153)는 "그냥 둬도 된다"로 회신 권장

진단서가 스스로 "그냥 둬도 되는 것인지 확인 필요"로 끝맺은 항목이다.

```java
UserTruck repTruck = userTruckService.getRepTruck(truckNo);
return repTruck != null ? ApiResponse.error("다른 사용자의 대표차량으로 등록된 차량번호입니다.") : ApiResponse.ok();
```

**차량 등록 "전"에 "이 차량번호가 이미 남의 대표차량인가"를 확인하는 용도**라, 타인 차량번호 입력이 정상 동작이다. 소유권을 검증하면 기능 자체가 성립하지 않는다. 반환값도 boolean 성격이고 타인 개인정보는 나가지 않는다.

남는 리스크는 차량번호 열거로 "등록 여부"를 알아낼 수 있다는 정도인데, 이는 소유권 검증이 아니라 rate limit 영역이다. `금주-작업-공수산정.md`의 "문제 아님" 판단과 일치한다.

---

## 4. BPABCTRANS-159 — 관제 메인 공지팝업 연계

**브랜치**: `feature/BPABCTRANS-159-popUp` (develop `8ffd6189`에서 분기)
**커밋**: `ef4e2fb3` → origin push → develop squash 머지 **`453883cf`** → push 완료
**결정 기록**: `.review-context/decisions.json` task `notice-popup-integration` (D001~D009) → `b7f41f4d`

관제시스템이 공지팝업을 등록/수정/삭제하면 bctrans가 받아 저장하고, 앱은 기존 조회 API로 읽는다.
※ BPABCTRANS-244(점검페이지 연계)와 **별개 기능**.

### 저장처가 `bctransdbx`가 아니다 — 계획 문서 정정

`금주-작업-공수산정.md` 43줄은 "`bctransdbx.popup` 테이블에 INSERT"로 적고 있으나 **사실이 아니다.**
앱 조회 API `GET /mobile/popup/list`(`PopupController`)가 보는 테이블은 **`chainportaldb.tb_a_popup_manage_i`**(`PopupMapper.xml:12`)다.
조회 API를 재사용하기로 한 이상 저장처도 같은 테이블이어야 하므로, **bctrans가 포털 DB에 쓰는 구조**가 됐다(사용자 승인).

### 엔드포인트 (관제팀 전달본)

```
POST /bctrans-api/popup/notice        인증 없음 (내부망, /maintenance/schedule 과 동일 패턴)

{ "action": "CREATE|UPDATE|DELETE",
  "data": { popupId, title, contentHtml, contentFormat, priority, beginDt, endDt, useAt } }

응답 data.popupId = 채번된 POPUP_ID → 관제가 보관했다가 UPDATE/DELETE에 실어야 함
```

### 결정 사항

| 논점 | 결정 | 근거 |
|---|---|---|
| 저장 위치 | `chainportaldb.tb_a_popup_manage_i` 직접 INSERT | 조회 로직 신규 개발 없이 재사용. 별도 테이블은 조회 쿼리 수정(회귀) + DDL 추가 필요 |
| 인증 | 없음 (내부망 전제) | `MaintenanceScheduleController`와 동일 패턴 |
| 이미지형 팝업 | **미지원** (HTML 본문만) | 사용자 지시. 이미지 업로드 경로 처리가 붙으면 공수 초과 |
| DELETE | `USE_AT='N'` 논리 삭제 | 포털 관리화면과 공유하는 테이블이라 물리 삭제 시 포털 이력 소실 |
| UPDATE/DELETE 대상 제한 | `FST_REGR_ID='CONTROL'` 관제 등록건만 | SYS_ID로 하려다 뒤집음 — 기존 앱 팝업이 **전부 `SYS_ID='09'`** 라 구분 불가 |
| `contentFormat` | 수신만 하고 저장 안 함 | 대응 컬럼 없음. 관제가 HTML 고정으로 보냄 |
| `priority` | `POPUP_PRIORITY` 컬럼 신설, **작을수록 상위** | 사용자 결정 |

### 실데이터로 확정한 고정값

기존 앱 팝업 전수 조회 결과 아래 조합이라 그대로 맞췄다.

`POPUP_TY='T'`(HTML형. 'H' 아님) / `SYS_ID='09'` / `WEB_USE_AT='N'` / `APP_USE_AT='Y'` / `USE_APP_VER='2'`

조회 쿼리가 `POPUP_TY='I'`만 이미지로 보고 나머지를 html 처리하는 구조라, 'T'가 HTML형 코드값이다.

### 정렬 규칙 변경 — 회귀 확인 대상

```sql
-- 변경 전: order by POPUP_BGNDE
   order by POPUP_PRIORITY, IFNULL(LST_UPD_DTM, FST_REG_DTM) desc, POPUP_ID desc
```

동일 우선순위면 수정일시 최신순이 위로 온다. `IFNULL`을 빼면 미수정 팝업(`LST_UPD_DTM` NULL)이 맨 뒤로 밀린다.
**관제 팝업뿐 아니라 기존 포털 등록 팝업의 노출 순서도 함께 바뀐다.** 앱 화면 회귀 확인 필요.

### 변경 파일

| 파일 | 내용 |
|---|---|
| `popup/vo/NoticePopupRequest.java` (신규) | `action` + `data` |
| `popup/vo/NoticePopupData.java` (신규) | 관제 data 필드 |
| `popup/vo/NoticePopup.java` (신규) | 테이블 매핑 VO |
| `popup/web/NoticePopupController.java` (신규) | `POST /popup/notice` |
| `popup/service/PopupService.java` | action 분기 + 검증 + 일시 파싱(ISO/공백 구분 모두 허용) |
| `popup/repository/PopupRepository.java` | insert/update/delete |
| `popup/mapper/PopupMapper.java` + `.xml` | INSERT / UPDATE / 논리삭제 + 조회 정렬 변경 |
| `db/migration/alter_popup_manage_add_priority.sql` (신규) | `POPUP_PRIORITY int NOT NULL DEFAULT 1` |

### 검증 상태

- ❌ **컴파일 미실행** (사용자 판단으로 생략 — IDE 빌드 예정)
- ❌ 테스트 미실행
- ❌ **DDL prod 미적용**

### ⚠ 배포 전 필수 — DDL 선행

`POPUP_PRIORITY` 없는 상태로 develop이 배포되면 **조회 쿼리가 `Unknown column`으로 터진다.**
관제 팝업만이 아니라 **기존 앱 팝업 조회(`GET /mobile/popup/list`)까지 전부 실패**한다. DDL이 먼저 나가야 한다.

---

## 5. 조사로 확정된 사실

### 5-1. `user_regist_truck.TRUCK_CODE`는 운송이벤트에 사용되지 않는다

읽는 경로가 **단 하나**다.

`UserTruckMapper.xml:14` `selectUserTrucks` → `UserTruckService.getUserTrucks()` → `UserTruckController:47` **`GET /mobile/user/truck`** (앱 차량목록 조회)

즉 **앱 화면 표출 전용**이고 서버 내부 로직에서 소비되지 않는다.

운송 흐름의 truckCode는 출처가 완전히 다르다:

| 쓰기 경로 | 출처 |
|---|---|
| `ContainerTransportDomainService:46,96,144,185` | `copino.get("truckCode")` — **COPINO 수신 메시지(외부)** |
| `GroupOrderUpdatePersistenceService:114,294`, `TssOpenApiApplicationService:175` | `truckDto.getTruckCode()` — **TSS 그룹오더 생성 요청(외부)** |
| `AllconeVbsTruckTransOrderService:306`, `BctransVbsTruckTransOrderService:547`, `OldVbsTruckTransOrderService:730,763` (블록체인 전송) | 저장된 `container_transport.TRUCK_CODE` 재사용 |

앱→서버 오더 요청 DTO에 `truckCode` 수신 필드가 없다. `allcone` 패키지의 truckCode는 전부 응답 DTO이거나 차량등록 관련이다.

**→ `금주-작업-공수산정.md` 미결 #1("유저별 트럭코드가 다를 때 오더에 실리는 기준 행")은 전제가 성립하지 않는다.** 유저별로 값이 달라도 운송 흐름에 충돌이 없다. (사용자 판단으로 해당 문서는 갱신하지 않음 — 이미 처리된 건이므로)

### 5-2. 앱은 deviceToken 헤더를 provider에서 자동 주입한다

```dart
// app-allcone/lib/core/blocs/initialization_bloc.dart:225
headers = {'deviceToken': fcmToken};
// :241
TruckApiProvider.instance.initialize(authority: authority, headers: headers);
```

`TruckApiProvider extends RestApi`이고, 개별 호출부는 헤더를 명시하지 않는다. 증거는 `getTruckDataList` — 서버 `GET /mobile/user/truck`이 `@RequestHeader("deviceToken")` 필수인데도 헤더 명시 없이 정상 동작 중이다.

**→ 일반 API는 서버에 인증을 추가해도 앱 수정 없이 동작한다.** `금주-작업-공수산정.md` 6번의 "`updateTruckCode`는 앱 수정 필요" 기재는 **사실이 아니다.**
단 `GET /certificate`(이미지 응답)는 예외 가능성이 있어 별도 확인이 필요하다(3항 참조).

### 5-3. 앱은 `driverId`를 전혀 보내지 않는다

`app-allcone/lib` 전체 grep 결과 **0건**. 서버의 `ObjectUtils.isEmpty(userId) ? driverId : userId` 패턴은 실질적으로 항상 `userId`만 탄다. 토큰 판정으로 치환해도 앱 영향이 없다.

(단, 앱 외 다른 클라이언트가 `driverId`를 쓰는지는 서버 코드만으로 확인 불가)

### 5-4. 앱의 트럭코드 수정 UI가 없다

`app-allcone/lib/truck/component/user_truck_component.dart:222`

```dart
Widget _buildTruckCode() {
  if (widget.truckData.truckCode!.isEmpty) {
    return InkWell(... onTap: 다이얼로그 → API 호출 ...);   // 최초 입력만 가능
  } else {
    return _buildTruckNo(widget.truckData.truckCode!);      // 텍스트 표시만, 탭 불가
  }
}
```

**트럭코드가 비어 있을 때만 "트럭 아이디 입력" 버튼이 뜨고, 한 번 값이 들어가면 수정할 경로가 없다.**

호출 체인 자체는 살아 있다: `user_truck_component.dart:236` → `UpdateTruckCodeDialog` → `updateUserTruckCode()`(204) → `truck_manage_bloc.dart:171` → `truck_api_provider.dart:31` → 서버

**앱에서 수정을 가능하게 하려면** else 분기를 if 분기처럼 `InkWell` + `UpdateTruckCodeDialog`로 감싸면 된다. 그 아래 체인과 서버는 전부 재사용 가능하다(`UPDATE` 쿼리라 최초/수정 동작이 동일).
다만 `UpdateTruckCodeDialog`(`update_truck_code_dialog.dart:10`)는 truckCode 초기값 파라미터가 없어서, 수정 화면인데 입력칸이 비어 뜬다. 초기값 파라미터 추가를 권장한다.

**이 앱 수정은 이번 작업 범위에서 제외됐다.**

### 5-5. `GET /certificate`는 앱도 allcone도 호출하지 않는다

| 확인 대상 | 결과 |
|---|---|
| `truck_api_provider.dart` 엔드포인트 9개 | GET certificate **없음** (`kUploadTruckCertUriKey`는 POST 업로드용) |
| `remote_config_keys.dart` 트럭 키 9개 | 이미지 **조회용 키 자체가 없음** — 앱은 `getUriStr(key)`로만 경로를 얻으므로 **호출 불가** |
| `truckRegCertImgLoc` | 모델(`truck_data.dart:15`)에는 있으나 `Image.network` 등으로 그리는 곳 없음 |
| `allcone` 리포 | `architecture.md`에 **"allcone은 certificate image stream을 추가하지 않는다"** 명시. wrap 대상 M-TRUCK 10건에서도 제외 |

즉 **소비자가 없는데 무인증으로 열려 있던 상태**였다. 인증 추가로 클라이언트 영향 없이 막았다.

엔드포인트 자체를 제거하는 선택지도 있었으나, 운영 실호출 여부를 코드로 확인할 수 없고(관제·어드민 등 미확인 소비자 가능성) 되돌리기 부담이 있어 **인증 추가로 진행**했다. 앞으로 앱이 차량등록증 이미지를 표시할 계획이 있다면 앱이 deviceToken 헤더를 실어야 한다.

### 5-6. 서버에 트럭코드 값 검증이 없다

검증은 앱의 `TruckTextUtils.instance.validateTruckCode()`(다이얼로그 line 32)에만 있다. 서버는 받은 값을 그대로 넣는다("거의 그대로 넣으면 된다"는 지시대로 유지). 인증이 붙어 타인 것은 못 바꾸지만, **본인 차량에 한해 앱 밖에서 형식에 안 맞는 값을 넣는 건 여전히 가능하다.** 서버 형식 검증 추가 여부는 미결.

---

## 6. 미확인 / 판단 대기

| # | 항목 | 내용 |
|---|---|---|
| 1 | `allcone` 리포의 정체 | `/mnt/d/workspace/bctrans_prod/allcone/` (`bpa/allcone/dev/allcone.git`)에 **동일한 `/mobile/user/truck/*` 엔드포인트가 재구현**되어 있음. README상 "올컨e 백엔드 분리 프로젝트"(Spring Boot 3.5 / Java 17). 이 리포와 레거시의 관계·전환 시점 불명 |
| 2 | 〃 취약점 이식 | 그 `TruckController`도 `@RequiresDeviceToken(validationMode = **NON_EMPTY**)`만 쓰고 파라미터 `userId`를 신뢰함. `updateTruckCode`(124)는 어노테이션 자체가 없음. **인터셉터에 `ValidationMode.REGISTERED`가 이미 구현돼 있으나 아무도 안 씀.** 레거시만 고치면 전환 시 취약점 부활 |
| 3 | `UserTruckService:145` 예외 로그 | 추가 여부 판단 대기 |
| 4 | `UserTruckMapper.xml:14~15` 중복 컬럼 | 제거 여부 판단 대기 |
| 5 | 서버 측 트럭코드 형식 검증 | 추가 여부 미결 |
| 6 | `POST /rep/manual` IP 제한 | 인프라 영역, 이번 범위 제외 |
| 7 | 앱의 차량등록증 이미지 표시 계획 | 있다면 앱이 `GET /certificate`에 deviceToken 헤더를 실어야 함. 계획 유무 미확인 |
| 8 | 264 나머지 7건(2~8번) 진행 | 미착수. 전부 같은 패턴(`getUserIdByDeviceTokenV2` 치환)이라 연속 처리 가능 |

---

## 7. 브랜치·커밋 현황

```
origin/develop
  8ffd6189  차량등록증 이미지 조회 API에 디바이스 토큰 인증 추가  ← BPABCTRANS-264 (squash)
  f20252e5  트럭코드 수정 API에 디바이스 토큰 인증 추가          ← BPABCTRANS-261 (squash)
  4c210f64  전체 토큰 대상 FCM 발송 제거                        ← BPABCTRANS-270 (squash)
  3efa2fec  Revert "hotfix: PNC PNIT 컴바인 내부통행로 이용 차단[임시]"
```

| 브랜치 | 상태 |
|---|---|
| `feature/BPABCTRANS-270-removeFCMSendForAll` | develop 반영 완료 (로컬·원격 존재) |
| `feature/BPABCTRANS-261-modifyTruckCode` | develop 반영 완료, origin push됨 (`71e9e43b`) |
| `feature/BPABCTRANS-264-appToAllconeDeviceToken` | `da9cf7c1` 커밋·origin push, **develop 반영 완료**. 나머지 7건 작업 시 이 브랜치 재사용 |

develop 직접 머지·push 방식으로 진행했으므로 MR은 생성하지 않았다.
모든 머지는 `--squash`로 처리해 develop에 커밋 1개씩만 남겼고, 매번 `git diff <브랜치> develop`으로 트리 일치를 검증했다.