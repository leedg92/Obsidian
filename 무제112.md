# 그룹오더 START 타임아웃: 미팅 말할 순서 (2026-09-18)

PPT `그룹오더-START-타임아웃-원인분석-v2.pptx` / 상세 `그룹오더-START-타임아웃-미팅자료-2026-09-16.md`

### 로그 추가 후 얻은것 
1. **어느 구간이 느린지 확정**: START 17구간 중 콜백매핑 배치 INSERT가 73행에 2.5초, 코피노 요청 INSERT 1.8초. 조회 구간은 전부 수십 ms. 범인이 "쓰기"라는 걸 여기서 잡았습니다.
2. **행당 단가**: 컨테이너 수와 소요가 정비례해 행당 14~16ms. 부하·시간대와 무관. 이 숫자가 예산 계산(100컨 7.3초, 147컨 10.1초)의 근거입니다.
3. **겹침 효과의 크기**: 리스너와 겹친 건과 안 겹친 건을 나눠 보니 중앙값 차이 5.6%. 겹침은 상시 원인이 아니라 가끔 튀는 꼬리라는 것.
4. **리스너 내부**: 컨테이너당 왕복 10회, 선사 매칭 조회가 항상 빈 결과. 리스너 수정안의 근거.
5. **락 대기 없음(오늘)**: 우선순위 UPDATE 구간이 631건에서 최대 18ms. 테스트베드의 락 현상이 운영에는 없다는 걸 이 로그로 확정했습니다.
6. **그룹오더 밖도 같은 병**: 같은 기간 SQL 로그와 합쳐 보니 한 행 INSERT 2.5초, 앱 조회 1,537건이 1초 초과. DB 단가 문제가 그룹오더 고유가 아니라는 것.

한 줄로: 진단 로그가 없었으면 "리스너 겹침이 원인"으로 결론 났을 텐데, 로그 덕에 "START 자신의 INSERT × DB 단가"로 바로잡았습니다.

### 결론적으로 뭘 해야하냐
- **코드(컨테인어스)**: 선사유효성 검증결과 매칭로직을 컨테이너마다 조회·삽입하던 걸 한 번 조회·일괄 삽입으로 바꿔 DB 점유를 12배 줄이고, START는 기본키를 UUIDv7로 바꿔 행마다 나는 디스크 읽기를 없앤다. 근본은 비동기 접수 API 배포로 10초 제한 자체를 없애는 것.
- **DB 설정(DBA, 무중단)**: 버퍼풀 32GB를 메모리 여유만큼 올리고 io_capacity·flush_neighbors를 SSD 값으로 바꿔 페이지 미스 횟수와 비용을 줄인다.
- **인스턴스·인프라(DBA, 작업 필요)**: 페이지 읽기 1회 2.35ms는 디스크 값이라 로컬 NVMe 이전이나 IOPS 상향, 그게 안 되면 메모리 증설과 183GB 다이어트로 버퍼풀 적중률을 올린다.
- **TMS(천일)**: CREATE 후 10초 텀을 두면 겹침은 사라지지만 147컨 이상은 못 막는다.

### 운영망의 환경은 뭘 했냐
1. **볼륨**: 운영 규모(콜백매핑 194만·코피노 125만 행)로 임시 테이블을 채워 같은 배치 INSERT 실행. 랜덤 UUID와 순차 키 두 벌로.
2. **버퍼풀 축소**: 128MB로 줄여 캐시 밖 상황 재현.
3. **왕복 지연**: DB 컨테이너에 tc netem 5ms를 걸어 앱↔DB 왕복 6~11ms. 이 상태로 A/B 실행.
4. **요청 재현**: 131의 천일 실제 172컨 오더로 CREATE JSON을 만들고, 응답 0.1초 뒤 START를 쏘는 천일 패턴 그대로.
## 1. 무슨 일이 나는가 (PPT 3장)
- 천일 TMS가 90컨 이상 오더를 CREATE 하고 0.05초 뒤 START. 서버 10초 제한을 넘겨 408.
- 408을 받아도 서버는 끝까지 처리해 오더는 START 상태로 남는다. 재생성하면 「컨테이너 중복」으로 거절.
- 30일 8건. 천일형 6건(98~133컨, 생성 직후 START), 경진형 2건(147·163컨, 몇 시간 뒤 START).

## 2. 흐름 두 줄기 (PPT 3장 그림)
- CREATE: 오더 저장 → 응답. 응답과 동시에 「생성됨」 이벤트를 던진다.
- 리스너: 이벤트를 받아 **다른 스레드에서** 컨테이너마다 검증행을 쓴다. 운영 1~9초. START 안에 있는 게 아니다.
- START: 자기 스레드에서 검증 → 블록체인 → 우선순위 UPDATE → 콜백매핑 INSERT.
- 천일은 리스너가 아직 도는 중에 START를 보낸다. 둘이 같은 DB를 동시에 쓴다. 이게 「겹침」이다. 다른 17개사는 텀을 두어 겹침이 없다.

## 3. 왜 10초를 넘는가 (PPT 4장)
- START가 쓰는 행: 컨테이너당 3행(코피노 2 + 콜백매핑 1). 100컨이면 300행.
- 운영 DB 행당 14~16ms. 페이지 읽기 1회 2.35ms × 미스 5~7회. DB 183GB에 버퍼풀 32GB(17%).
- 같은 코드·같은 요청·같은 볼륨을 테스트베드에서 돌리면 행당 1.2ms. 단가 차이가 10배 넘는다.
- 예산: 고정비 0.7초 + 컨테이너당 0.045초 + 블록체인 0.5초. 100컨 7.3초, 133컨 9.4초, 147컨 10.1초.
- 겹치면 그 위에 얹힌다. 운영 로그로 재보면 중앙값 5.6%, 가끔 2~3초 꼬리.
- 1초 넘는 INSERT 857건은 예외 없이 랜덤 UUID 키 테이블. 그룹오더 밖에서도 한 행 INSERT가 1~2.5초.

## 4. 이번에 만든 코드 수정 = 리스너 (PPT 5·7·8장)
- 현행: 컨테이너 98개를 하나씩 돌며 매번 키 조회 → INSERT, 검증행 조회 → INSERT, 선사 매칭 조회. 984왕복, 전부 한 트랜잭션.
- 수정: 컨테이너 정보 조회 1번 → 키 한 번에 조회 → 없는 것만 배치 INSERT → 검증행 배치 INSERT → 선사 결과 count 1번(0이면 매칭 건너뜀). 6왕복, 트랜잭션은 문장마다.
- 효과: 리스너 6.5초 → 0.5초. 결과는 동일(검증행 196 · 콜백매핑 98행, 선사 매칭 20/20 대조).
- 이게 START에 주는 것: 직접 단축은 없다. 겹침이 사라져 꼬리와 락 위험이 없어진다.
- 컨테인어스에 넘기기 전 손볼 곳 3건 있음(전배 시 count 범위, 재생성 시 존재 판정, 배치 INSERT 실패 처리).

## 5. A/B 결과에서 말할 것 (PPT 9·11장)
- 유효: 리스너 6.5 → 0.5초.
- 정정: 「코드만으로 START 절반」은 테스트베드 고유 현상. 검증행 FK가 마스터 행에 공유 락을 걸고 START의 우선순위 UPDATE가 리스너 끝날 때까지 6.5초 기다린 것. 락을 빼고 다시 돌리면 A 7.67초 · B 7.80초로 같다.
- 운영은 진단 로그 631건에서 이 UPDATE가 최대 18ms. 운영 START에 락 대기는 없다.
- 단 운영에도 같은 FK가 있다(131 확인). 왜 안 걸리는지는 미확인. DBA 확인 한 줄:
  `SHOW GLOBAL VARIABLES LIKE 'foreign_key_checks'; SHOW GLOBAL VARIABLES LIKE 'tx_isolation';`
- 구조상 위험은 있고 테스트베드에서 재현됐으니, 리스너 트랜잭션 분할이 그 위험도 같이 없앤다.

## 6. START 자체를 줄이는 것 (PPT 5·12장)
- 코드(컨테인어스)
  1. 리스너 일괄화 + 트랜잭션 분할 (위 4번, 패치 있음)
  2. UUIDv7: 새 테이블 5개 기본키 생성기만 교체(38곳). 타입·스키마 그대로. 행마다 나는 페이지 미스 제거. 코드 쪽에서 단가를 내리는 유일한 수단.
  3. START 조회 6개를 트랜잭션 하나로: 고정비 0.7 → 0.05초.
  4. 비동기 접수 API 운영 배포: 10초 제한 자체가 없어짐. develop에 있고 운영 미배포.
- DB 설정(무중단): 버퍼풀 32GB → 메모리 여유만큼, io_capacity 200 → 2000, flush_neighbors 0, 슬로우 로그 ON.
- DB 인프라: 스토리지(페이지 읽기 2.35ms, 로컬 NVMe면 20배 감소), 메모리 증설, 183GB 다이어트.
- TMS(천일): CREATE 후 10초 텀. 겹침은 없애지만 147컨 이상은 못 막음.

## 7. 반문 대비
- 코드 고치면 천일형 사라지나 → 완화. 100컨 9.6 → 6.6초, 133컨 9.4 → 8.8초라 여유 1초. 확실한 건 DB 단가·UUIDv7·비동기 접수.
- 150컨도 되나 → 코드만으로는 안 됨. 행당 16ms × 300행 = 4.8초가 START 안에 그대로 남는다.
- 테스트베드는 왜 빨랐나 → 페이지 미스가 없어서. 지연 주입은 왕복만 흉내내고 행당 비용은 못 만든다.
- 09-12 이후 0건이면 나아진 것 아닌가 → 천일의 100컨 이상 START가 1/4로 줄어서다(기대 0.6건).
- 행 락 아니냐 → 운영 로그에선 없다. 테스트베드에선 있었다. 트랜잭션 분할이 어느 쪽이든 해결.

## 8. 한 줄
코드의 낭비(왕복 984회 · 중복 기록 · 랜덤 UUID)와 DB의 느린 단가(페이지 미스 × 2.35ms)가 곱해져 10초를 넘긴다. 리스너 수정은 겹침을 없애고, START 자체는 UUIDv7·조회 묶기·비동기 접수와 DB 단가 개선이 맡는다.


